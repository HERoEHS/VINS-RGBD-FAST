# 가드 리셋 경로 점검 체크리스트 (2026-08-03)

> 누적 상태에 의존하는 가드는, 그 상태를 리셋하는 경로가 **병리 조건이 지속되는
> 동안에도** 발생할 수 있으면 무력화된다. EDIE와 SW2가 서로 모르는 채 같은 버그
> 계보를 반복한 것이 실증 근거다(아래 사례). 새 가드를 설계하거나 기존 가드를
> 리뷰할 때 이 체크리스트를 적용한다.

## 문제 정의

가드의 전형적 구조는 "누적 상태가 문턱을 넘으면 개입"이다. 여기서 누적 상태란
카운터(연속 발동 횟수), 비율(제거율·정화율), streak(연속 무이상), 앵커(기준
위치·기준선), 쿨다운(재발화 억제 시각) 등이다. 이 상태들은 어딘가에서 리셋된다 —
명시적 초기화, 재기동, 저장소 소멸, 분모 재계산. **리셋 자체는 정상 동작이지만,
리셋 조건이 병리 조건과 독립이 아니면 병리가 리셋을 유발해 가드를 영구 회피한다.**

## 실증 사례 (버그 3건 + 예방 성공 1건)

### 사례 1 — EDIE `179807f`: 자기참조 리셋 (2026-07)

4선 에스컬레이션 가드는 "정화(무이상 solve) 없는 연속 절제 3회 → 조기 재초기화"인데,
초기 구현은 정화 판정에 cum_hit(누적 변위 가드 발동)를 포함했다. 누적 가드가 환원을
수행하는 중의 solve는 주행 문턱 아래로 내려가 '무이상'으로 오판되고, **에스컬레이션
카운터가 리셋**된다. 결과: 중간 강도 폭주가 4선을 영구 회피할 수 있었다.
수정 = 정화 판정에서 cum_hit 제외.

**패턴: 리셋 트리거(정화 판정)가 가드 자신의 발동 부산물(cum_hit로 인한 환원)에
오염 — 자기참조 루프.**

### 사례 2 — SW2 lifelong 3.67: 세션 경계 비영속 (2026-07)

노드 은퇴 가드의 조건 "제거율 ≥ 50%"는 "원래 희박한 노드"(보호 대상)와 "사멸로
빈 노드"(배제 대상)를 구분하려는 것이다. 그런데 삭제된 특징은 재기동 시 DB에서
로드 자체가 안 되어 `removed` 배열이 전부 False로 초기화된다 — **제거율이 세션마다
0으로 리셋**된다. 지난 세션에 60%를 비운 노드가 원래 희박한 노드로 위장되어,
가드가 걸러내려던 대상과 보호하려던 대상이 뒤섞인다. 은퇴 4조건 중 잔존 word 수는
이미 세션 간 누적인데 제거율만 비대칭이었다 — 의도된 보수성이 아니라 누락.
수정 = 아카이브 테이블을 분모·분자에 합산해 세션 간 누적으로 전환(13줄 diff).

**패턴: 세션/재기동 경계에서 상태가 영속되지 않아, 가드의 의미가 "누적 조건"에서
"세션 내 조건"으로 몰래 변질.**

### 사례 3 — SW2 lifelong 3.64: 저장소 수명 < 상태 수명 (2026-07)

구역 쿨다운(600s)이 Khronos 객체 레코드 안에 저장돼 있었다. 객체가 변화감지 한
라운드에서 안 보이면 신선도 규칙이 레코드를 pop하는데, **쿨다운이 레코드째
증발**한다. 결과: 600s 쿨다운인데 같은 구역이 15s 간격으로 4회 중복 발화.
커버리지 경로는 복셀(공간) 키 쿨다운이라 면역 — 객체 경로만 취약.

**패턴: 상태를 담은 엔티티의 수명이 상태의 의도 수명보다 짧다. 시간 의미의 상태를
소멸 가능한 객체에 매달면 안 된다.**

### 사례 4 (예방 성공) — EDIE z 래칫 가드의 앵커 계승 (2026-07-31)

정지 창 재진입마다 앵커를 새로 잡으면(재래치) 다리 이벤트가 만든 z 계단이 새 앵커에
구워져 래칫을 지울 수 없게 된다 — 앵커 리셋이 병리(z 계단)를 합법화하는 구조.
처방 = 앵커 '계승': 리셋(신규 앵커)을 병리와 독립적인 조건(휠 병진 없음 + 다리각
2-LSB 복귀, xy는 추가로 gyro 순적분 <5°)으로 게이팅하고, 조건 미충족 시 이전 앵커를
계승한다. **이 클래스의 버그를 설계 단계에서 차단한 사례** — 체크리스트 Q3·Q4를
사실상 선취 적용한 것.

## 체크리스트

가드 설계·리뷰 시 순서대로 답한다:

- **Q1. 누적 상태 전수 나열** — 이 가드가 의존하는 누적 상태를 전부 나열했는가?
  (카운터 / 비율 / streak / 앵커 / 기준선 / 쿨다운 / 래치 플래그)
- **Q2. 리셋·소멸 경로 전수 나열** — 각 상태에 대해 누가·언제 그것을 0으로
  만들거나 재계산하는가?
  - 명시적 리셋 코드 (`streak = 0`, `clearState()`)
  - 재기동·재초기화·세션 경계 (메모리 상태 소실, DB 리로드)
  - 저장소 소멸 (레코드 pop, 맵 erase, 노드 소멸)
  - 분모·기준의 재계산 (비율의 분모가 바뀌면 사실상 리셋)
- **Q3. 병리 중 리셋 가능성** — 각 리셋 경로에 대해: 가드가 막으려는 병리 조건이
  **지속되는 동안에도** 그 리셋이 발생할 수 있는가? 가능하다면 그것이 곧 가드
  회피 경로다. (사례 1·2·3 전부 여기서 걸린다)
- **Q4. 리셋 트리거의 독립성** — 리셋 조건이 가드가 감시하는 신호, 또는 가드
  발동의 부산물과 독립인가? 가드 개입이 리셋 조건을 만족시키면 자기참조 루프다.
  (사례 1: 환원 중 solve가 '정화'로 보임 / 사례 4: 계승 조건을 외부 센서
  합의로만 구성해 독립성 확보)
- **Q5. 세션 경계 영속성** — 상태가 세션·재기동을 넘어 누적돼야 의미가 있다면
  실제로 영속되는가? 영속 안 된다면 가드는 "세션 내 조건"으로 의미가 바뀌는데,
  그것이 의도인가? (사례 2)
- **Q6. 저장소 수명** — 상태를 담은 엔티티의 수명이 상태의 의도 수명 이상인가?
  시간·공간 의미의 상태는 시간·공간 키에 저장하고, 소멸 가능한 객체 레코드에
  매달지 않는다. (사례 3)
- **Q7. 최악 조합 회귀 테스트** — "병리 지속 + 리셋 발생"의 최악 시나리오를
  gtest로 고정했는가? 정상 경로 테스트만으로는 이 클래스가 안 잡힌다 —
  사례 1은 리뷰 재검증("논리 확실?")에서, 사례 2는 별도 실험(3.66)에서야 발견됐다.

## EDIE 적용 — 점검 후보 목록

아래는 현재 트리의 가드들이 가진 누적 상태 목록이다. **각 항목의 Q1~Q7 대입은
후속 작업이며, 코드 재확인 없이 버그로 단정하지 않는다** (점검 완료된 것은 표기):

| 가드 | 누적 상태 | 점검 상태 |
|---|---|---|
| 4선 에스컬레이션 | 연속 절제 streak, 정화 판정 | ✅ 사례 1로 발견·수정(`179807f`) |
| IMU/휠 스탬프 간극 가드(09-04) | 콜백 `last_imu_t`/`last_wheel_t`, `estimator.reset_request_`(atomic), `tracker_reset_request_` | 리셋 경로: 콜백은 요청만, 백엔드 `consumeResetRequest()`가 시드 캡처→clearState→feature_buf 비움→트래커 리셋. **한계(Q3)**: 이미지 불연속 경로는 별도 리셋(시드 무캡처·`last_imu_t=0`)이라 이미지·IMU 동시 점프(NTP 스텝) 시 순서 경쟁 — 통일은 이연. **갭(Q7)**: 후방 점프 통합 실증 0건(전방은 GON/GOFF 쌍으로 실증). 휠 가드는 기본 끔. |
| 누적 변위 가드 (xy/z) | 앵커 위치, 정지 연속 카운터 | ✅ 계승 설계로 선취(사례 4), v13 검증 |
| bgz_lock | 잠금 상태, 수렴 판정 이력 | ✅ 08-03 점검: clearState가 6상태 전부 리셋(locked·tracker·rest·잠금값·재잠금시각) — 낡은 잠금 생존 없음 |
| gaugeSlideGuard | 연속 발동 카운터(절제 조건) | 부분 — 사례 1 수정이 이 카운터의 리셋 경로였음, 잔여 경로 전수는 미확인 |
| z 앵커 계승 배선 | 계승 판정 입력(다리각·yaw 진단값) | ✅ 08-03 점검: clearState 미리셋이나 anchor_history_valid_=false가 선행 게이트라 stale 읽기 불가(첫 래치가 전량 덮어씀). ⚠️Q4 주의: anchor_net_yaw_rad_는 (gyr−Bgs.z) 적분이라 Bg_z 인플레 병리 시 오염 가능 — 실패 방향은 보수적(신규 래치=미소각, 안전 무해). Q7 갭: 배선부 gtest 미커버(순수 함수만), 재생 A/B로 대체 검증 |
| StillMotionFactor | 정지 판정 연속성 | 미점검 |
| 정지 yaw 래칫 가드 | 앵커 yaw·raw 적분·경과시간(래치 동기 리셋 3종), rest 이력(bgz_rest 공유) | ✅ 08-04 설계 단계 적용: 리셋 3경로(clearState/래치/정지 해제) 전부 병리(게이지 슬라이드=상태만 회전)와 독립 — still 판정은 휠 기반이라 슬라이드가 리셋을 못 만들고, 발동(역회전)도 앵커·적분·rest에 무영향(Q4: 기준=정지 실측 gyro 중앙값, Bgs 자기참조는 [YAW-DEV] 진단으로 적발 후 절연). yaw 앵커는 의도적 무계승(주행 실회전이 계승 허용오차를 오염 — z와 반대 결정, 근거 코드 주석). ⚠️알려진 창: 래치 지연 15 solve 중 슬라이드는 앵커에 구워짐(큰 스텝은 per-solve 절제 관할). Q7 갭: 순수 함수만 gtest(3건), 배선부는 obs_v2 ON/OFF 재생 A/B 대체. 부산물 교훈: RestBias::median()은 빈 이력 UB — 호출부는 반드시 ready() 게이트 뒤에서만(08-04 진단 크래시 실증) |
| 재초기화 pose 시드 계승 | T_seed·다리 gyro 적분·캡처 스냅샷(세션 관통), 시드 재료 3종(정화 pose·앵커 래치 시각·첫 절제 시각, 세션 스코프) | ✅ 08-04 설계+A/B 적용: Q6 핵심 사례 — T_seed류는 clearState 생존이 존재 이유(캡처→clearState→재init 관통), 재료는 캡처가 clearState보다 선행하므로 세션 리셋 안전. Q4: 앵커 시드는 '오염 실증(절제 이력) 존재 AND 래치<첫 절제'일 때만 — 무절제 failure에서 앵커 채택하던 초판 결함을 강제 reboot A/B가 적발(0.56m 낡은 시드→GT 2.2배), 오염 증거 게이트로 수정. 다리 적분은 dt 위생 가드(0<dt<0.1s) — 세션 경계 epoch dt가 무리셋 적분 오염(A/B 실증 -1.16e10deg). Q7: 순수 수학 gtest 5 + 강제 reboot 재생 A/B(경계 점프 6mm·GT 연속). ⚠️한계: 무오염(강제) 시나리오만 실증 — 앵커 경로(실폭주)는 ⓐ bag/실기 후속 |
| 워밍업 init 게이트 | streak 3종·예산 카운터·래치 2종(ready/fell_back)+배선 로그 래치 2종 | ✅ 08-03 설계 단계 적용: 병리(움직임) 중 리셋은 게이트를 계속 닫아둠(회피 역방향=안전), reset 트리거(clearState)는 발동 부산물과 독립(미초기화선 failure 미발동=자기참조 불가), 무한 보류는 예산 폴백 차단(gtest ④·vg2 실증). ⚠️한계: READY 래치~init 시도 갭의 이벤트는 부분 오염 가능(기존 대비 개선), 입력 사각=데드밴드 이하 이동·비다리 외란(v2 후보: gyro 정지 조건). Q7 갭: 배선부는 gtest 미커버(재생 vg1·vg2 대체) |

점검 시 주의: 코드 상태 주장 전 반드시 현재 코드를 확인한다
(과거 세션 기록·이 문서의 표는 stale할 수 있다).

## 출처

- EDIE: 커밋 `179807f`(정화 판정 cum_hit 제외), z 래칫 가드 앵커 계승(미커밋 워킹트리,
  2026-07-31 HANDOFF)
- SW2: `HERoEHS/alice_navigation` `feature/m2_camera` 브랜치
  `localization/lifelong_slam/sessions/` 3.64·3.66·3.67 — 조사 경위는
  [SW2_LIFELONG_SLAM_SURVEY.md](SW2_LIFELONG_SLAM_SURVEY.md)
